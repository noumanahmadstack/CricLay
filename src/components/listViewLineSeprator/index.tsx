import { FC } from "react";
import { View } from "react-native";
import styles from "./styles";
const ListViewLineSeprator: FC = () => {
    return (
        <View style={styles.container} />
    )
}
export default ListViewLineSeprator